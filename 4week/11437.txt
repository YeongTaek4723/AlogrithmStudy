최소통통조상은 LCA함수를 사용해서 트리의 두 원소의 공통이 되는 조상들 중 
깊이가 가장 깊은 조상을 찾아내면 되는 알고리즘이다. 




1)q.push(1);    check[1] = 1;   시작 노드를 넣어주고 push
2)depth[a[x][i]] = depth[x] + 1; 정점과의 거리 저장
  check[a[x][i]] = 1;            체크
  parent[a[x][i]] = x;           현재정점의 부모노드는 x임

3)함수 LCA는 두 노드를 비교한 뒤 레벨이 다르다면 같아질 때까지 레벨이 큰 것을 위로 올린다. 
3-1) u = parent[u];     레벨이 같아질 때 까지 부모노드를 넣어준다
     u = parent[u];
    v = parent[v];      두 정점이 같아질 때까지 부모노드를 넣어준다