주어진 숫자의 범위내의 소수를 찾아야하는 문제로 범위내의 소수를 찾아내는 에라토스네스의 체 알고리즘을 사용하여 문제를 풀 수 있다.

1)소수의 범위가 되는 m, n을 입력받는다.
2)boolean 형 arr를 생성하고 0과 1은 소수가 아니므로 true로 설정한다.
3)i가 2부터 Math.sqrt(arr.length)까지 i를 반복한다.
4)만약 arr[i]가 true라면 소수가 아니므로 그 다음 for문으로 가지 않고 위 for문으로 가서 반복한다.
5)i가 소수라면 i의 배수 값들은 소수가 아니므로 arr[j]의 값을 true로 설정한다.
6)기존에 정한 범위 m~n까지 중 true가 아닌 값을 출력한다.