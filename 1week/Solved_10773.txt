자바는 자바에서 지원하는 Stack가 있어서 C언어 처럼 다 짜는것이 아닌 import를 이용해서 Stack을 사용할 수 있는데 그것을 이용하여 주어진 문제를 풀면 된다.

1)정수의 개수를 K로 입력 받는다.
2)반복문을 이용하여 num에 정수를 입력받고, 만약 num이 0이라면 
  stack에 맨위의 정수를 pop으로 제거한다.
  0이 아니라면 stack에 push로 정수를 추가한다.
3)자바의 for each문을 사용하여 stack에 남아 있는 정수를 sum에 더한 후 출력해 준다.