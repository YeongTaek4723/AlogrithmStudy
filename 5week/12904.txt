주어진 두 문자열이 같아지는 경우 1 아닌 경우 0을 출력하는 문제로 문자열 S를 T로 만드는 것보다 
T를 S로 만드는 것이 쉽기에 그 방법과 그리디 알고리즘을 사용해서 해결 할 수 있다.


1) 문자열 S, T를 입력받고 while문을 통해서 비교한다.
2)만약 T[T.length() - 1]의 단어가 A이면 pop_back()를 통해 제거하고 아니면 
맨뒤의 단어를 제거 후 reverse를 통해 문자열을 뒤집는다. 
3)만약 S와 T의 길이가 같을때, 두 문자열이 같으면 1을 아니면 0을 출력 후 while문을 나간다.